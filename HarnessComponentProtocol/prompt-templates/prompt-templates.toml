kind = "prompt-template"
product = "capability"
slot = "prompt-template"
autoload = true
name = "prompt-templates"
description = "Reusable prompt templates with parameter substitution and formatting"
source = "pi"

[assumption]
compensates = "Reusable, parameterized prompts keep task-specific phrasing consistent across invocations rather than relying on the model to reconstruct it each time."
rationale = "inferred"  # README frames templates as data-not-code for maintainability, not as a model limitation
calibrated_for = ["claude-sonnet-4-5", "claude-opus-4-5"]
review_trigger = "model-change"
load_bearing = "unmeasured"
eval_scenarios = []
