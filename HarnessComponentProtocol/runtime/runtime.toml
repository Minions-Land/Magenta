kind = "runtime"
product = "capability"
slot = "runtime:process"
autoload = true
name = "process"
description = "Process runtime provider with Magenta1 portable sandbox guardrails ported to TypeScript."
source = "magenta"
bundles = ["sandbox:magenta"]
origin = "magenta1-general-harness"
origin_rel = "ported/adapted"
target = "runtime://process"
impl = ["runtime/magenta/process-runtime.ts"]

[assumption]
compensates = "Safety boundary: portable guards (path-traversal, workspace cwd, env allowlist, network deny/allowlist, wall-clock timeout) enforce limits the model cannot be trusted to respect. Boundary persists regardless of model capability."
rationale = "stated"  # [policy].portable_guards + validatePolicy() throws in magenta/process-runtime.ts
calibrated_for = ["any"]
review_trigger = "never"
load_bearing = "verified"  # portable guards enforced; OS-level backend not-ported (see [policy].os_enforcement)
eval_scenarios = []

[policy]
portable_guards = ["direct-exec gate", "workspace cwd", "environment allowlist", "wall-clock timeout", "filesystem read/write checks for declared paths", "network deny tag checks", "network allowlist checks for declared URL/host inputs"]
os_enforcement = "not-ported"
notes = "Magenta1 runtime_provider.rs also had sandbox-exec/bwrap backends. This TS provider currently enforces portable guards only."
