# ripgrep runtime asset

This directory contains the platform-specific `rg` binaries embedded in Magenta release builds. It is host runtime support under `_magenta`, not an HCP module or protocol role.
