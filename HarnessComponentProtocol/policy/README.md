# Policy Providers

Approval and shell command policy providers migrated from Magenta1
`approval_provider.rs` and `shell_policy_provider.rs`.

These providers return policy decisions and classifications only. They do not
prompt the user and do not execute shell commands.
