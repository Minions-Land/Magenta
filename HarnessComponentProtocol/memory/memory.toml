kind = "memory"
product = "capability"
slot = "memory"
autoload = false
name = "memory"
description = "Experimental session-grounding memory provider; not connected to default coding-agent context."
source = "magenta"

[assumption]
compensates = "If a host explicitly connects storage and context injection, memory can retain and recall user constraints, preferences, and project facts between runs."
rationale = "inferred"  # provider describes the intended role; default coding-agent does not consume it
calibrated_for = ["claude-sonnet-4-5", "claude-opus-4-5"]
review_trigger = "model-change"
load_bearing = "unmeasured"  # explicit-selection experiment; no default session-grounding consumer
eval_scenarios = []
