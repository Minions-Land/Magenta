kind = "tool"
product = "tool"
name = "web-fetch"
description = "Fetch a web page using the Magenta process-tool source."
source = "magenta"
sources = ["magenta"]
autoload = true
origin = "magenta1-general-harness"

[source_config.magenta]
implementation_manifest = "magenta/web-fetch.toml"
requires = ["runtime:process", "sandbox"]
