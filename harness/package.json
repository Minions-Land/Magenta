{
	"name": "@magenta/harness",
	"version": "0.0.1",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "tsc -p tsconfig.build.json && shx mkdir -p dist/skills/bundled && shx cp -r skills/bundled/* dist/skills/bundled/",
		"test": "vitest --run"
	},
	"dependencies": {
		"@earendil-works/pi-agent-core": "0.80.2",
		"@earendil-works/pi-ai": "0.80.2"
	},
	"devDependencies": {
		"shx": "0.4.0",
		"@types/node": "24.12.4",
		"typescript": "5.9.3",
		"vitest": "4.1.9"
	}
}
