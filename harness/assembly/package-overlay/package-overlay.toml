kind = "assembly"
name = "package-overlay"
description = "Harness package overlay discovery and profile selection. Actual packages live in the repository-level packages/ root."

[exports]
module = "assembly/package-overlay/package-overlay.ts"
